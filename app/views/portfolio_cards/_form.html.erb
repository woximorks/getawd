<%= form_with(model: portfolio_card) do |form| %>
  <% if portfolio_card.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(portfolio_card.errors.count, "error") %> prohibited this portfolio_card from being saved:</h2>

      <ul>
        <% portfolio_card.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :portfolio_card_title, style: "display: block" %>
    <%= form.text_field :portfolio_card_title %>
  </div>

  <div>
    <%= form.label :portfolio_card_description, style: "display: block" %>
    <%= form.text_area :portfolio_card_description %>
  </div>

  <div>
    <%= form.label :portfolio_card_image_url, style: "display: block" %>
    <%= form.text_field :portfolio_card_image_url %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
