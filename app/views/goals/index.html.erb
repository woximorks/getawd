<p style="color: green"><%= notice %></p>

<h1>Goals</h1>

<div id="goals">
  <% @goals.each do |goal| %>
    <p>
      <%= goal.title %>
    </p>
    <p>
      <%= link_to "Show this goal", goal %>
    </p>
  <% end %>
</div>

<%= link_to "New goal", new_goal_path %>
